"""Learning content layer.

Educational content is application data: it lives in the backend, never in
ChemEngine and never in the frontend. This package holds the seeded
development content in an isolated, typed module that can later migrate to a
database/CMS without changing the frontend or the learning API contract.
"""
