# Backend

This directory will contain the Chemora backend API (FastAPI + PostgreSQL).

The backend consumes ChemEngine and exposes the API layer used by the frontend applications (mobile, web, admin).

> **Status**: Reserved — not yet implemented. This is the next milestone (Backend Foundation).
